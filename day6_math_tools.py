#第六天项目：数学工具包
print("数学工具包")
print("=" * 40)

#1.1. 定义各种数学函数
def 加法(a,b):
    return a + b

def 减法(a,b):
    return a - b

def 乘法(a,b):
    return a * b

def 除法(a,b):
    if b == 0:
        return "错误：除数不能为零"
    return a / b

def 判断质数(n):
    if n <= 1:
        return False
    for i in range(2,int(n ** 0.5) + 1):
        if n % i == 0:
            return False
    return True

def 计算阶乘(n):
    if n < 0:
        return "错误：负数没有阶乘"
    elif n == 0:
        return 1
    else:
        结果 = 1
        for i in range(1,n + 1):
            结果 *= i
        return 结果
    
def 转换温度(摄氏度):
    """将摄氏度转换为华氏度"""
    华氏度 = 摄氏度 * 9/5 + 32
    return 华氏度

def 判断闰年(年份):
    """判断是否是闰年"""
    if 年份 % 400 == 0:
        return True
    elif 年份 % 100 == 0:
        return False
    elif 年份 % 4 == 0:
        return True
    else:
        return False

# 2. 测试函数
def 测试所有函数():
    """测试所有数学函数"""
    print("开始测试数学工具包...")
    print("=" * 40)
    
    print(f"1. 加法测试：10 + 5 = {加法(10, 5)}")
    print(f"2. 减法测试：10 - 5 = {减法(10, 5)}")
    print(f"3. 乘法测试：10 × 5 = {乘法(10, 5)}")
    print(f"4. 除法测试：10 ÷ 5 = {除法(10, 5)}")
    print(f"   除法测试：10 ÷ 0 = {除法(10, 0)}")
    
    print(f"5. 质数测试：")
    print(f"   7是质数吗？{'是' if 判断质数(7) else '不是'}")
    print(f"   8是质数吗？{'是' if 判断质数(8) else '不是'}")
    
    print(f"6. 阶乘测试：")
    print(f"   5的阶乘 = {计算阶乘(5)}")
    print(f"   0的阶乘 = {计算阶乘(0)}")
    
    print(f"7. 温度转换测试：")
    print(f"   0°C = {转换温度(0):.1f}°F")
    print(f"   100°C = {转换温度(100):.1f}°F")
    
    print(f"8. 闰年测试：")
    print(f"   2000年是闰年吗？{'是' if 判断闰年(2000) else '不是'}")
    print(f"   1900年是闰年吗？{'是' if 判断闰年(1900) else '不是'}")
    print(f"   2024年是闰年吗？{'是' if 判断闰年(2024) else '不是'}")
    
     
#3.数学包主程序
def 主程序():

    while True:
        print("\n请选择要使用的工具:")
        print("1. 四则运算")
        print("2. 判断质数")
        print("3. 计算阶乘")
        print("4. 温度转换")
        print("5. 判断闰年")
        print("6. 测试所有函数")
        print("7. 退出")

        选择 = input("请输入数字选择：")

        if 选择 == "1":
            print("\n🔢 四则运算")
            a = float(input("请输入第一个数："))
            b = float(input("请输入第二个数："))
            print(f"{a} + {b} = {加法(a, b)}")
            print(f"{a} - {b} = {减法(a, b)}")
            print(f"{a} × {b} = {乘法(a, b)}")
            print(f"{a} ÷ {b} = {除法(a, b)}")
            
        elif 选择 == "2":
            print("\n🔍 判断质数")
            n = int(input("请输入一个整数："))
            if 判断质数(n):
                print(f"{n} 是质数")
            else:
                print(f"{n} 不是质数")
                
        elif 选择 == "3":
            print("\n📈 计算阶乘")
            n = int(input("请输入一个整数："))
            print(f"{n}! = {计算阶乘(n)}")
            
        elif 选择 == "4":
            print("\n🌡️ 温度转换")
            摄氏度 = float(input("请输入摄氏度："))
            华氏度 = 转换温度(摄氏度)
            print(f"{摄氏度}°C = {华氏度:.2f}°F")
            
        elif 选择 == "5":
            print("\n📅 判断闰年")
            年份 = int(input("请输入年份："))
            if 判断闰年(年份):
                print(f"{年份}年是闰年")
            else:
                print(f"{年份}年不是闰年")
                
        elif 选择 == "6":
            测试所有函数()
            
        elif 选择 == "7":
            print("👋 退出数学工具包，再见！")
            break
            
        else:
            print("❌ 请输入1-7之间的数字")

# 4. 启动程序
if __name__ == "__main__":
    主程序()